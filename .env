# Server configuration
SERVER_PORT='8080'       # Port on which the server will run
SERVER_HOST='0.0.0.0'    # Host address (0.0.0.0 means accessible from all network interfaces)

# Database configuration (PostgreSQL)
DB_HOST='localhost'      # Database host (use an IP or domain in production)
DB_PORT='5432'           # PostgreSQL default port
DB_USER='dba'            # Database username
DB_PASSWORD='#Delta@0711' # Database password (keep secure, avoid committing to git)
DB_NAME='test'           # Name of the database
DB_SSLMODE='disable'     # SSL mode (disable for local dev, prefer 'require' in production)

# JWT configuration
JWT_SECRET='HOhVjGVpFacS7jrHbFk75sOInqyZjh6Tf1mUHIFvuHM='  # Secret key used to sign JWT tokens

# Application environment
ENV='development'        # Application environment (development, staging, production)